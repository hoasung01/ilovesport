<section id="admin-product__show">
  <table class="table table-striped">
    <thead class="thead-dark">
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @sport.name %></td>
      </tr>
    </tbody>
  </table>

  <div class="mt-3">
    <%= link_to 'Edit', edit_admin_sport_path(@sport), class: 'btn btn-primary me-2' %>
    <%= link_to 'Back', admin_sports_path, class: 'btn btn-secondary' %>
  </div>
</section>
