<section id="facility-owner-training-ground__show">
  <table class="table table-striped">
    <thead class="thead-dark">
      <tr>
        <th>Name</th>
        <th>Sport</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @training_ground.name %></td>
        <td><%= @training_ground.sport.name %></td>
      </tr>
    </tbody>
  </table>

  <div class="mt-3">
    <%= link_to 'Edit', edit_facility_owner_training_ground_path(@training_ground), class: 'btn btn-primary me-2' %>
    <%= link_to 'Back', facility_owner_training_grounds_path, class: 'btn btn-secondary' %>
  </div>
</section>
